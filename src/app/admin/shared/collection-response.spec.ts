import { CollectionResponse } from './collection-response';

describe('CollectionResponse', () => {
  it('should create an instance', () => {
    expect(new CollectionResponse()).toBeTruthy();
  });
});
